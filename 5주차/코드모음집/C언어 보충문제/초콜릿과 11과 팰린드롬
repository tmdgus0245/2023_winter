#include <stdio.h>
int main() {
	int T = 5, a, result, temp;
	scanf("%d", &T);
	for (T; T > 0; T--) {
		scanf("%d", &a);
		if (a == 1) {
			printf("0\n");
		}
		else {
			printf("1");
			for (a -= 2; a > 0; a--) {
				printf("2");
			}
			printf("1\n");
		}
	}
}
